# Anthropic — used by lib/anthropic.ts (new Anthropic() reads this automatically)
ANTHROPIC_API_KEY=sk-ant-...

# Vercel Postgres — used by lib/db.ts via @vercel/postgres
# For local development, a regular Postgres connection string works.
POSTGRES_URL=postgres://user:password@host:5432/devdoc
